-- โหลด Wind UI Library
local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()

-- สร้างหน้าต่าง UI หลัก
local Window = WindUI:CreateWindow({
    Title = "Drak Hub | Evade script by Tatar v0.10",
    Icon = "shield",
    Author = "Tatar",
    Folder = "DrakHub_Evade",
    Size = UDim2.fromOffset(580, 460),
    Transparent = true,
    Theme = "Dark",
    SideBarWidth = 170,
    HasOutline = true
})

-- สร้าง Tab หลัก
local MainTab = Window:Tab({ Title = "ฟังก์ชันหลัก", Icon = "home" })
local ESPTab = Window:Tab({ Title = "ระบบมองทะลุ (ESP)", Icon = "eye" })
local UtilityTab = Window:Tab({ Title = "ระบบเสริม", Icon = "settings" })

-- ตัวแปรควบคุมสถานะและสี
local _G = _G
_G.TokenFarm = false
_G.AutoJump = false
_G.PlayerSpeed = 16
_G.NoclipEnabled = false
_G.ESP_Players = false
_G.ESP_Bots = false
_G.PlayerESPColor = Color3.fromRGB(80, 255, 120)
_G.BotESPColor = Color3.fromRGB(255, 60, 60)

-- Service พื้นฐาน
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

-- ==================== TAB 1: ฟังก์ชันหลัก (Main) ====================
MainTab:Toggle({
    Title = "ฟาร์มโทเค็นออโต้ (Token Farm)",
    Desc = "วาร์ปเก็บเงินอัตโนมัติ โดยไม่ติดชนรถหรือสิ่งกีดขวาง",
    Value = false,
    Callback = function(Value)
        _G.TokenFarm = Value
        
        task.spawn(function()
            local originalPos = nil
            local safePlatform = nil
            
            while _G.TokenFarm do
                task.wait(0.6)
                pcall(function()
                    local character = LocalPlayer.Character
                    if character and character:FindFirstChild("HumanoidRootPart") then
                        local rootPart = character.HumanoidRootPart
                        
                        local targetToken = nil
                        for _, obj in pairs(workspace:GetDescendants()) do
                            if not _G.TokenFarm then break end
                            local name = string.lower(obj.Name)
                            if (name:find("token") or name:find("money") or name:find("cash") or name:find("pickup")) and not name:find("car") and not name:find("vehicle") and not name:find("bus") then
                                if obj:IsA("ProximityPrompt") and obj.Parent then
                                    local parentName = string.lower(obj.Parent.Name)
                                    if not parentName:find("car") and not parentName:find("vehicle") then
                                        targetToken = obj.Parent
                                        break
                                    end
                                elseif obj:IsA("BasePart") then
                                    targetToken = obj
                                    break
                                end
                            end
                        end
                        
                        if targetToken then
                            if not originalPos then
                                originalPos = rootPart.CFrame
                            end
                            
                            if not safePlatform or not safePlatform.Parent then
                                safePlatform = Instance.new("Part")
                                safePlatform.Size = Vector3.new(20, 1, 20)
                                safePlatform.Position = rootPart.Position + Vector3.new(0, 1000, 0)
                                safePlatform.Anchored = true
                                safePlatform.Transparency = 0.5
                                safePlatform.Parent = workspace
                            end
                            
                            rootPart.CFrame = targetToken.CFrame + Vector3.new(0, 5, 0)
                            task.wait(0.2)
                            
                            for _, p in pairs(targetToken:GetDescendants()) do
                                if p:IsA("ProximityPrompt") then
                                    fireproximityprompt(p)
                                end
                            end
                            task.wait(0.2)
                            
                            if originalPos then
                                rootPart.CFrame = originalPos
                            end
                        end
                    end
                end)
            end
            
            if safePlatform then
                safePlatform:Destroy()
            end
        end)
    end
})

-- หน้าต่างลอย (Floating GUI) สำหรับ Auto Jump - ดีไซน์พรีเมียม
local JumpGui = Instance.new("ScreenGui")
JumpGui.Name = "DrakHubJumpGui"
JumpGui.ResetOnSpawn = false
JumpGui.Enabled = false
JumpGui.Parent = game.CoreGui

local JumpFrame = Instance.new("Frame")
JumpFrame.Size = UDim2.new(0, 190, 0, 80)
JumpFrame.Position = UDim2.new(0.5, -95, 0.12, 0)
JumpFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 22)
JumpFrame.BorderSizePixel = 0
JumpFrame.Active = true
JumpFrame.Draggable = true
JumpFrame.Parent = JumpGui

local JumpCorner = Instance.new("UICorner")
JumpCorner.CornerRadius = UDim.new(0, 10)
JumpCorner.Parent = JumpFrame

local JumpStroke = Instance.new("UIStroke")
JumpStroke.Color = Color3.fromRGB(50, 50, 65)
JumpStroke.Thickness = 1.5
JumpStroke.Parent = JumpFrame

local JumpTitle = Instance.new("TextLabel")
JumpTitle.Size = UDim2.new(1, 0, 0, 30)
JumpTitle.BackgroundTransparency = 1
JumpTitle.Text = "⚡ Auto Jump (v0.10)"
JumpTitle.TextColor3 = Color3.fromRGB(220, 220, 230)
JumpTitle.TextSize = 12
JumpTitle.Font = Enum.Font.GothamBold
JumpTitle.Parent = JumpFrame

local InnerJumpToggleBtn = Instance.new("TextButton")
InnerJumpToggleBtn.Size = UDim2.new(0.85, 0, 0, 34)
InnerJumpToggleBtn.Position = UDim2.new(0.075, 0, 0.42, 0)
InnerJumpToggleBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
InnerJumpToggleBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
InnerJumpToggleBtn.Text = "Status: OFF"
InnerJumpToggleBtn.TextSize = 12
InnerJumpToggleBtn.Font = Enum.Font.GothamBold
InnerJumpToggleBtn.Parent = JumpFrame

local JumpBtnCorner = Instance.new("UICorner")
JumpBtnCorner.CornerRadius = UDim.new(0, 8)
JumpBtnCorner.Parent = InnerJumpToggleBtn

local isJumpActive = false
InnerJumpToggleBtn.MouseButton1Click:Connect(function()
    isJumpActive = not isJumpActive
    if isJumpActive then
        InnerJumpToggleBtn.Text = "Status: ON"
        InnerJumpToggleBtn.TextColor3 = Color3.fromRGB(100, 255, 130)
        InnerJumpToggleBtn.BackgroundColor3 = Color3.fromRGB(30, 70, 45)
    else
        InnerJumpToggleBtn.Text = "Status: OFF"
        InnerJumpToggleBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
        InnerJumpToggleBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
    end
end)

task.spawn(function()
    while true do
        task.wait(0.05)
        if isJumpActive then
            pcall(function()
                local character = LocalPlayer.Character
                if character and character:FindFirstChild("Humanoid") then
                    local humanoid = character.Humanoid
                    if humanoid.FloorMaterial ~= Enum.Material.Air then
                        humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
                    end
                end
            end)
        end
    end
end)

MainTab:Toggle({
    Title = "เปิดหน้าต่างลอย Auto Jump (GUI)",
    Desc = "แสดงปุ่มควบคุมการกระโดดรัวบนหน้าจอ",
    Value = false,
    Callback = function(Value)
        JumpGui.Enabled = Value
        if not Value then
            isJumpActive = false
            InnerJumpToggleBtn.Text = "Status: OFF"
            InnerJumpToggleBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
            InnerJumpToggleBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
        end
    end
})

-- หน้าต่างลอย (Floating GUI) สำหรับ Dash - ดีไซน์พรีเมียม
local DashGui = Instance.new("ScreenGui")
DashGui.Name = "DrakHubDashGui"
DashGui.ResetOnSpawn = false
DashGui.Enabled = false
DashGui.Parent = game.CoreGui

local DashFrame = Instance.new("Frame")
DashFrame.Size = UDim2.new(0, 220, 0, 125)
DashFrame.Position = UDim2.new(0.5, -110, 0.25, 0)
DashFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 22)
DashFrame.BorderSizePixel = 0
DashFrame.Active = true
DashFrame.Draggable = true
DashFrame.Parent = DashGui

local DashCorner = Instance.new("UICorner")
DashCorner.CornerRadius = UDim.new(0, 10)
DashCorner.Parent = DashFrame

local DashStroke = Instance.new("UIStroke")
DashStroke.Color = Color3.fromRGB(50, 50, 65)
DashStroke.Thickness = 1.5
DashStroke.Parent = DashFrame

local DashTitle = Instance.new("TextLabel")
DashTitle.Size = UDim2.new(1, 0, 0, 30)
DashTitle.BackgroundTransparency = 1
DashTitle.Text = "🚀 Dash Controller (v0.10)"
DashTitle.TextColor3 = Color3.fromRGB(220, 220, 230)
DashTitle.TextSize = 12
DashTitle.Font = Enum.Font.GothamBold
DashTitle.Parent = DashFrame

local SpeedBox = Instance.new("TextBox")
SpeedBox.Size = UDim2.new(0.85, 0, 0, 32)
SpeedBox.Position = UDim2.new(0.075, 0, 0.32, 0)
SpeedBox.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
SpeedBox.TextColor3 = Color3.fromRGB(255, 255, 255)
SpeedBox.PlaceholderText = "ความเร็ว Dash"
SpeedBox.Text = "45"
SpeedBox.TextSize = 12
SpeedBox.Font = Enum.Font.Gotham
SpeedBox.Parent = DashFrame

local BoxCorner = Instance.new("UICorner")
BoxCorner.CornerRadius = UDim.new(0, 6)
BoxCorner.Parent = SpeedBox

local InnerDashToggleBtn = Instance.new("TextButton")
InnerDashToggleBtn.Size = UDim2.new(0.85, 0, 0, 34)
InnerDashToggleBtn.Position = UDim2.new(0.075, 0, 0.64, 0)
InnerDashToggleBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
InnerDashToggleBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
InnerDashToggleBtn.Text = "Status: OFF"
InnerDashToggleBtn.TextSize = 12
InnerDashToggleBtn.Font = Enum.Font.GothamBold
InnerDashToggleBtn.Parent = DashFrame

local BtnCorner = Instance.new("UICorner")
BtnCorner.CornerRadius = UDim.new(0, 8)
BtnCorner.Parent = InnerDashToggleBtn

local isDashingActive = false
InnerDashToggleBtn.MouseButton1Click:Connect(function()
    isDashingActive = not isDashingActive
    if isDashingActive then
        InnerDashToggleBtn.Text = "Status: ON"
        InnerDashToggleBtn.TextColor3 = Color3.fromRGB(100, 255, 130)
        InnerDashToggleBtn.BackgroundColor3 = Color3.fromRGB(30, 70, 45)
    else
        InnerDashToggleBtn.Text = "Status: OFF"
        InnerDashToggleBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
        InnerDashToggleBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
    end
end)

task.spawn(function()
    local dashBodyVel = nil
    while true do
        task.wait(0.03)
        pcall(function()
            local character = LocalPlayer.Character
            if character and character:FindFirstChild("HumanoidRootPart") and character:FindFirstChild("Humanoid") then
                local rootPart = character.HumanoidRootPart
                local humanoid = character.Humanoid
                local camera = workspace.CurrentCamera
                
                if isDashingActive then
                    if not dashBodyVel or dashBodyVel.Parent ~= rootPart then
                        if dashBodyVel then dashBodyVel:Destroy() end
                        dashBodyVel = Instance.new("BodyVelocity")
                        dashBodyVel.MaxForce = Vector3.new(400000, 0, 400000)
                        dashBodyVel.Parent = rootPart
                    end
                    
                    local customSpeed = tonumber(SpeedBox.Text) or 45
                    if customSpeed > 60 then customSpeed = 60 end
                    
                    local camLook = camera.CFrame.LookVector
                    local flatLook = Vector3.new(camLook.X, 0, camLook.Z).Unit
                    
                    dashBodyVel.Velocity = flatLook * (customSpeed * 1.35)
                    
                    if humanoid.FloorMaterial == Enum.Material.Air and rootPart.Velocity.Y > 5 then
                        rootPart.Velocity = Vector3.new(rootPart.Velocity.X, -10, rootPart.Velocity.Z)
                    end
                else
                    if dashBodyVel then
                        dashBodyVel:Destroy()
                        dashBodyVel = nil
                    end
                end
            end
        end)
    end
end)

MainTab:Toggle({
    Title = "เปิดหน้าต่างพุ่งไปข้างหน้า (Floating GUI)",
    Desc = "พุ่งตามทิศทางหน้าจอ (ดีไซน์พรีเมียม)",
    Value = false,
    Callback = function(Value)
        DashGui.Enabled = Value
        if not Value then
            isDashingActive = false
            InnerDashToggleBtn.Text = "Status: OFF"
            InnerDashToggleBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
            InnerDashToggleBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
        end
    end
})

-- ==================== TAB 2: ระบบมองทะลุ (ESP) ====================
local function createESP(targetModel, nameText, color, isBot)
    if not targetModel then return end
    
    local root = targetModel:FindFirstChild("HumanoidRootPart") or targetModel.PrimaryPart or targetModel:FindFirstChildWhichIsA("BasePart")
    if not root then return end

    local highlight = targetModel:FindFirstChild("EvadeESP_Highlight")
    if not highlight then
        highlight = Instance.new("Highlight")
        highlight.Name = "EvadeESP_Highlight"
        highlight.Adornee = targetModel
        highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
        highlight.FillTransparency = 0.5
        highlight.OutlineTransparency = 0.2
        highlight.Parent = targetModel
    end
    highlight.FillColor = color

    local billboard = targetModel:FindFirstChild("EvadeESP_Billboard")
    if not billboard then
        billboard = Instance.new("BillboardGui")
        billboard.Name = "EvadeESP_Billboard"
        billboard.Adornee = root
        billboard.Size = UDim2.new(0, 160, 0, 40)
        billboard.StudsOffset = Vector3.new(0, 3.5, 0)
        billboard.AlwaysOnTop = true
        billboard.Parent = targetModel

        local textLabel = Instance.new("TextLabel")
        textLabel.Name = "ESPText"
        textLabel.Size = UDim2.new(1, 0, 1, 0)
        textLabel.BackgroundTransparency = 1
        textLabel.TextStrokeTransparency = 0
        textLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
        textLabel.TextSize = isBot and 15 or 12
        textLabel.Font = Enum.Font.GothamBold
        textLabel.Parent = billboard
    end

    local textLabel = billboard:FindFirstChild("ESPText")
    if textLabel then
        textLabel.TextColor3 = color
    end
end

local function clearESP(targetModel)
    if not targetModel then return end
    local hl = targetModel:FindFirstChild("EvadeESP_Highlight")
    if hl then hl:Destroy() end
    local bb = targetModel:FindFirstChild("EvadeESP_Billboard")
    if bb then bb:Destroy() end
end

RunService.RenderStepped:Connect(function()
    pcall(function()
        local myChar = LocalPlayer.Character
        local myRoot = myChar and myChar:FindFirstChild("HumanoidRootPart")
        if not myRoot then return end

        clearESP(myChar)

        if _G.ESP_Players then
            for _, player in pairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and player.Character then
                    local char = player.Character
                    local targetRoot = char:FindFirstChild("HumanoidRootPart")
                    if targetRoot then
                        createESP(char, player.DisplayName, _G.PlayerESPColor, false)
                        local bb = char:FindFirstChild("EvadeESP_Billboard")
                        if bb and bb:FindFirstChild("ESPText") then
                            local dist = math.floor((myRoot.Position - targetRoot.Position).Magnitude)
                            bb.ESPText.Text = string.format("%s\n[%d เมตร]", player.DisplayName, dist)
                        end
                    end
                end
            end
        else
            for _, player in pairs(Players:GetPlayers()) do
                if player.Character then clearESP(player.Character) end
            end
        end

        if _G.ESP_Bots then
            for _, obj in pairs(workspace:GetDescendants()) do
                if obj:IsA("Model") and obj ~= myChar then
                    local isPlayerChar = false
                    for _, p in pairs(Players:GetPlayers()) do
                        if p.Character == obj then isPlayerChar = true break end
                    end
                    
                    if not isPlayerChar and (obj:FindFirstChild("Humanoid") or obj:FindFirstChild("Nextbot") or string.lower(obj.Name):find("bot") or string.lower(obj.Name):find("ai")) then
                        local targetRoot = obj:FindFirstChild("HumanoidRootPart") or obj.PrimaryPart
                        if targetRoot then
                            createESP(obj, "⚠️ " .. obj.Name .. " ⚠️", _G.BotESPColor, true)
                            local bb = obj:FindFirstChild("EvadeESP_Billboard")
                            if bb and bb:FindFirstChild("ESPText") then
                                local dist = math.floor((myRoot.Position - targetRoot.Position).Magnitude)
                                bb.ESPText.Text = string.format("⚠️ %s ⚠️\n[ห่าง: %d เมตร]", obj.Name, dist)
                            end
                        end
                    end
                end
            end
        else
            for _, obj in pairs(workspace:GetDescendants()) do
                if obj:IsA("Model") and obj:FindFirstChild("EvadeESP_Billboard") then
                    local isPlayerChar = false
                    for _, p in pairs(Players:GetPlayers()) do
                        if p.Character == obj then isPlayerChar = true break end
                    end
                    if not isPlayerChar then clearESP(obj) end
                end
            end
        end
    end)
end)

ESPTab:Toggle({
    Title = "มองทะลุบอท / Nextbot (ESP Bots)",
    Desc = "ไฮไลท์สีเตือนภัย + แสดงระยะห่างระหว่างเรากับบอท",
    Value = false,
    Callback = function(Value)
        _G.ESP_Bots = Value
    end
})

pcall(function()
    ESPTab:Colorpicker({
        Title = "เปลี่ยนสี ESP บอท (Bot ESP Color)",
        Default = Color3.fromRGB(255, 60, 60),
        Callback = function(Value)
            _G.BotESPColor = Value
        end
    })
end)

ESPTab:Divider()

ESPTab:Toggle({
    Title = "มองทะลุผู้เล่นอื่น (ESP Players)",
    Desc = "ไฮไลท์สีผู้เล่นอื่น + แสดงชื่อและระยะห่าง",
    Value = false,
    Callback = function(Value)
        _G.ESP_Players = Value
    end
})

pcall(function()
    ESPTab:Colorpicker({
        Title = "เปลี่ยนสี ESP ผู้เล่น (Player ESP Color)",
        Default = Color3.fromRGB(80, 255, 120),
        Callback = function(Value)
            _G.PlayerESPColor = Value
        end
    })
end)

-- ==================== TAB 3: ระบบเสริม (Utility) ====================
UtilityTab:Slider({
    Title = "ความเร็วผู้เล่น (WalkSpeed)",
    Desc = "ปรับความเร็วการวิ่งในเกม",
    Value = 16,
    Min = 16,
    Max = 150,
    Callback = function(Value)
        _G.PlayerSpeed = Value
    end
})

RunService.RenderStepped:Connect(function()
    pcall(function()
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
            LocalPlayer.Character.Humanoid.WalkSpeed = _G.PlayerSpeed
        end
    end)
end)

UtilityTab:Slider({
    Title = "Gravity Control (Workspace Gravity)",
    Desc = "ปรับแรงโน้มถ่วงของเกม",
    Value = 196,
    Min = 0,
    Max = 300,
    Callback = function(Value)
        workspace.Gravity = Value
    end
})

-- ระบบบิน (Fly)
local flying = false
local flySpeed = 5
local bodyVelocity, bodyGyro

local function startFly()
    local character = LocalPlayer.Character
    if not character then return end
    local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if not humanoidRootPart or not humanoid then return end

    flying = true
    humanoid.PlatformStand = true

    bodyVelocity = Instance.new("BodyVelocity")
    bodyVelocity.Velocity = Vector3.new(0, 0, 0)
    bodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
    bodyVelocity.Parent = humanoidRootPart

    bodyGyro = Instance.new("BodyGyro")
    bodyGyro.P = 9e4
    bodyGyro.MaxTorque = Vector3.new(9e4, 9e4, 9e4)
    bodyGyro.CFrame = humanoidRootPart.CFrame
    bodyGyro.Parent = humanoidRootPart

    task.spawn(function()
        local camera = workspace.CurrentCamera
        local uis = game:GetService("UserInputService")
        
        while flying do
            task.wait()
            local moveDirection = Vector3.new(0, 0, 0)
            local forward = camera.CFrame.LookVector
            local right = camera.CFrame.RightVector
            
            if uis:IsKeyDown(Enum.KeyCode.W) or uis:IsKeyDown(Enum.KeyCode.Up) then
                moveDirection = moveDirection + forward
            end
            if uis:IsKeyDown(Enum.KeyCode.S) or uis:IsKeyDown(Enum.KeyCode.Down) then
                moveDirection = moveDirection - forward
            end
            if uis:IsKeyDown(Enum.KeyCode.A) or uis:IsKeyDown(Enum.KeyCode.Left) then
                moveDirection = moveDirection - right
            end
            if uis:IsKeyDown(Enum.KeyCode.D) or uis:IsKeyDown(Enum.KeyCode.Right) then
                moveDirection = moveDirection + right
            end
            
            bodyVelocity.Velocity = moveDirection * (flySpeed * 10)
            bodyGyro.CFrame = camera.CFrame
            humanoidRootPart.RotVelocity = Vector3.new(0, 0, 0)
        end
    end)
end

local function stopFly()
    flying = false
    local character = LocalPlayer.Character
    if character then
        local humanoid = character:FindFirstChildOfClass("Humanoid")
        if humanoid then
            humanoid.PlatformStand = false
        end
    end
    if bodyVelocity then bodyVelocity:Destroy() end
    if bodyGyro then bodyGyro:Destroy() end
end

UtilityTab:Toggle({
    Title = "เปิด/ปิด ระบบบิน (Fly)",
    Desc = "บินได้อย่างอิสระตามทิศทางกล้อง",
    Value = false,
    Callback = function(Value)
        if Value then
            startFly()
        else
            stopFly()
        end
    end
})

UtilityTab:Slider({
    Title = "ความเร็วในการบิน (Fly Speed)",
    Desc = "ปรับความเร็วตอนบิน",
    Value = 5,
    Min = 1,
    Max = 10,
    Callback = function(Value)
        flySpeed = Value
    end
})

LocalPlayer.CharacterAdded:Connect(function()
    if flying then
        stopFly()
    end
end)

-- ระบบทะลุกำแพง (Noclip)
UtilityTab:Toggle({
    Title = "ทะลุกำแพง (Noclip)",
    Desc = "เดินทะลุสิ่งกีดขวางและกำแพงในแผนที่",
    Value = false,
    Callback = function(Value)
        _G.NoclipEnabled = Value
    end
})

RunService.Stepped:Connect(function()
    if _G.NoclipEnabled then
        pcall(function()
            local char = LocalPlayer.Character
            if char then
                for _, part in pairs(char:GetDescendants()) do
                    if part:IsA("BasePart") then
                        part.CanCollide = false
                    end
                end
            end
        end)
    end
end)

-- แจ้งเตือนเมื่อโหลดเสร็จ
WindUI:Notify({
    Title = "Drak Hub v0.10",
    Content = "โหลดทุกฟังก์ชันและระบบเสริมสำเร็จเรียบร้อยแล้ว!",
    Duration = 5
})
